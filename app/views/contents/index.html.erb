<h1>Contents</h1>

<%= form_tag(contents_path, method: :get) do %>
    <div class="field">
        <%= label_tag :tags %>
        <%= select_tag 'tags[]', options_for_select(current_user.tags.pluck(:name), params[:tags]), { class: "js-example-tokenizer", multiple: 'multiple', style: 'width: 40%'}%>
    </div>

    <div class="action">
        <%= submit_tag %>
    </div>

    <br>
    <br>

<% end %>

<% @contents.each do | content | %>
    <%= link_to 'Show', content_path(content) %>
    <%= link_to 'Edit', edit_content_path(content) %>
    <%= link_to 'Delete', content_path(content), method: :delete, data: { confirm: 'Are you sure, you want to delete!'} %>
    <strong>Title:</strong> <%= content.title %><br>
    <strong>Description: </strong> <%= content.description %> </br>
    <strong>Tags:</strong> <%= content.tags.pluck(:name).join(', ')%>
 


 </br>
 </br>
<% end %>